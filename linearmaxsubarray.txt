MAX-SUBARRAY(low, high, A)
if  low == high
    return (low, high, A[low])
else 
    (low', high', sum') = M-S(low, high-1, A)
    (i, sum'') = max(sum(A, low+i, high)) 
    if sum'' > sum'
        return(i, high, sum'')
    else
        return(low', high', sum')
    